syntax = "proto3";
option go_package = "github.com/shaunnope/go-jaguard/server"


message CreateRequest {
    string path = 1;
    string data[] = 2;
    string flags[] = 3;
}

message CreateResponse {}

service server {
    rpc Create(CreateRequest) returns (CreateResponse);
}